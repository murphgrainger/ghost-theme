# Custom Ghost Journal Theme

### Run build & watch for changes
```bash
yarn dev
```
Edit `/assets/css/` files, which are compiled to `/assets/built/` automatically using:

### Zip theme file for upload to Ghost
```bash
yarn zip
```
The zip file is generated in the `/dist` directory. Upload this into the Ghost GUI.